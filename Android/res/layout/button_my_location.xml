<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/location_button_container"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <Button
        android:id="@+id/my_location_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/button_me" />

    <Button
        android:id="@+id/drone_location_button"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="5dp"
        android:layout_below="@id/my_location_button"
        android:background="@drawable/button_plane_direction" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/zoom_to_fit_button"
        style="@style/Fab.MyLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/drone_location_button"
        android:src="@drawable/ic_expand"
        android:visibility="gone"
        tools:visibility="visible" />
</RelativeLayout>